[{
        "type": "function",
        "function": {
            "name": "get_web_elements",
            "description": "此工具用來獲取某個網頁上所有可點擊按鈕",
            "parameters": {
                "type": "object",
                "properties": {
                    "url": { "type": "string", "description": "單一網頁連結" }
                },
                "required": ["url"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "close_page",
            "description": "此工具用來關閉你之前開啟的網頁(需提供uuid)，確保你在每次使用完網頁後，都調用此工具",
            "parameters": {
                "type": "object",
                "properties": {
                    "uuid": { "type": "string", "description": "先前使用goto_page所獲得的uuid" }
                },
                "required": ["uuid"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "goto_page",
            "description": "此工具用來開啟一個網頁，並獲得該page的uuid (用於之後調用其他工具)",
            "parameters": {
                "type": "object",
                "properties": {
                    "url": { "type": "string", "description": "單一網頁連結" }
                },
                "required": ["url"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "read_file",
            "description": "此工具用來閱讀某個某個已知路徑的本地檔案",
            "parameters": {
                "type": "object",
                "properties": {
                    "path": { "type": "string", "description": "在使輸入檔案的路徑" }
                },
                "required": ["path"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "send_to_channel",
            "description": "此工具用來在某個discord頻道傳送訊息，必須先知道頻道的ID",
            "parameters": {
                "type": "object",
                "properties": {
                    "text": { "type": "string", "description": "在使輸入你要對這個頻道的人說什麼話，確保內容與提示詞內所提到的風格一致" },
                    "channelID": { "type": "string", "description": "在此輸入**完整**的頻道ID" },
                    "replied": { "type": "bool", "description": "如果你需要特別標記這是回覆對方的訊息，請傳入True" },
                    "messageID": { "type": "string", "description": "如果你需要特別標記這是回覆對方的訊息，請傳入該**完整**訊息的 messageID。" }
                },
                "required": ["text", "channelID"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "self_database_save",
            "description": "此工具用來將你查到的，或者是任何你看到的事情存起來，以後可以使用self_database_search來查詢",
            "parameters": {
                "type": "object",
                "properties": {
                    "text": { "type": "string", "description": "在此輸入你要儲存的事情" }
                },
                "required": ["text"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "self_database_search",
            "description": "此工具用來查詢你以前搜尋到的資訊，或者你以前認為任何需要記得的事情",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": { "type": "string", "description": "在此輸入搜尋關鍵字" },
                    "num": { "type": "integer", "description": "在此輸入要獲得幾個結果，預設為3個" }

                },
                "required": ["query", "num"]
            }
        }
    }
]